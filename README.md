# Homebrew Tap for Sociable Weaver

Install the current supported Sociable Weaver release with:

```sh
brew install albertattard/tap/sw
```

The formula supports Apple Silicon macOS and x86_64 Linux. It installs
target-specific archives published from verified GitHub Releases; Rust is not
required on the installing machine.
